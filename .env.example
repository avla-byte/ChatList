# Скопируйте в .env и подставьте реальные значения.
# Ключи API в БД не хранятся: в таблице `models` в поле api_id указывается
# ИМЯ переменной (например OPENAI_API_KEY), а значение читается отсюда.

# Пример:
# OPENAI_API_KEY=sk-...
# DEEPSEEK_API_KEY=...
